# 미니 ERP 개발 지시서

> **용도**: 이 문서를 Claude Code에 그대로 붙여넣거나 프로젝트 폴더에 `SPEC.md`로 저장한 뒤,
> "이 지시서대로 개발해줘. 개발 단계(11장) 순서대로 진행하고, 단계마다 완료 보고해줘."라고 지시한다.

---

## 1. 프로젝트 개요

- **제품명**: 미니 ERP (가칭 — 배포 시 변경 가능)
- **목적**: 상용 ERP를 도입하기 어려운 중소기업·소상공인을 위한 **무료 업무 관리 프로그램**
- **대상 사용자**: 5~30인 규모 회사의 사무·회계 담당자. 컴퓨터 비전문가 기준으로 설계한다.
- **배포 방식**: GitHub에 오픈소스(MIT 라이선스)로 무료 배포. 설치 과정 없이 파일을 받아 바로 사용.
- **포함 범위**: 거래처·품목·재고·매입·매출·배송·경비 관리, **수금/지급 관리(부분 수금, 미수금,
  잔금, 거래처 원장)**, 회계 리포트(손익 요약), 견적서·거래명세표 발행,
  **증빙(영수증) 파일 관리와 세무사 전달용 자료 패키지 생성**,
  **자체기장용 장부(매출장·매입장·현금출납부, 일계/월계/누계, 연도 마감·이월)**
- **제외 범위(중요)**: 세금 신고, 전자세금계산서 국세청 연동, 4대보험 요율 계산은 **만들지 않는다**.
  (매년 바뀌는 법령·요율에 대한 유지보수 책임을 지지 않기 위함. 부가세 10% 단순 계산까지만 지원)

## 2. 기술 요구사항

- **배포 형태는 2가지**로 하며, 같은 코드 하나에서 만들어낸다:
  - **A. 웹형(1차)**: 서버가 필요 없는 로컬 웹 애플리케이션. **단일 HTML 파일 1개**로 번들.
    브라우저에서 더블클릭으로 실행. Chrome의 "바로가기 만들기 → 창으로 열기"를 사용하면
    별도 작업 없이도 독립 창 프로그램처럼 쓸 수 있다(README에 안내 포함).
  - **B. 설치형 프로그램(2차)**: 같은 코드를 **Electron으로 감싸 Windows용 .exe 설치 파일**로 만든다.
    electron-builder(NSIS)로 설치 파일 생성, 앱 아이콘·프로그램명 지정.
    자동 업데이트는 만들지 않는다(새 버전은 GitHub에서 받도록 안내).
- **저장 계층 추상화(중요)**: 파일 저장/읽기는 반드시 어댑터 패턴으로 분리한다.
  ```js
  // storage 어댑터 인터페이스: pickFolder, readFile, writeFile, listFiles, deleteFile
  // - 브라우저: File System Access API (showDirectoryPicker)
  // - Electron: preload + IPC로 Node fs 사용 (폴더 선택은 dialog.showOpenDialog)
  ```
  앱 코드는 어댑터만 호출하고, 실행 환경을 감지해 알맞은 어댑터를 쓴다.
  이렇게 하면 웹형과 설치형이 동일한 데이터 폴더·파일 형식을 공유한다(서로 호환).
- 개발 중에는 파일을 나눠도 되지만(html/css/js 분리), 빌드 스크립트로 반드시 단일 HTML도 만들 것.
- 외부 CDN 의존 최소화. 앱 본체는 의존성 0의 순수 HTML/CSS/JavaScript로 작성
  (Electron·electron-builder는 설치형 빌드에만 사용하는 개발 의존성).
- **지원 브라우저(웹형)**: Chrome, Edge (File System Access API 필요). 미지원 브라우저에서는
  자동 저장 대신 수동 JSON 내려받기/불러오기로 동작하는 **폴백 모드**를 제공하고 안내 배너를 띄운다.
  설치형은 브라우저 제약이 없다.
- **한국어 UI**. 금액은 `1,234,567` 형식(toLocaleString), 날짜는 `YYYY-MM-DD`.
- 코드에는 **한국어 주석**을 충분히 달 것 (배포 후 다른 사람이 수정하기 쉽도록).

## 3. 데이터 저장 방식 (핵심 설계)

사용자는 PC에 **구글드라이브 데스크탑**을 설치해 두었다. 프로그램은 로컬 폴더에 저장만 하면
구글드라이브가 알아서 클라우드 동기화·백업을 해주는 구조다.

1. 최초 실행 시 "데이터 폴더 연결" 버튼 → `showDirectoryPicker()`로 사용자가
   구글드라이브 안의 폴더를 선택한다 (`mode: "readwrite"`).
2. 폴더 핸들은 **IndexedDB**에 저장해 다음 실행 때 재사용한다.
   재실행 시 권한이 만료되어 있으면 "폴더 다시 연결" 버튼을 배너로 보여주고,
   클릭(사용자 제스처) 시 `requestPermission()`으로 복구한다.
3. 데이터가 바뀔 때마다 **자동 저장**: 500ms 디바운스 후 `erp-data.json`을 폴더에 덮어쓴다.
   저장 성공 시 사이드바에 "저장됨 HH:MM:SS" 표시, 실패 시 빨간 표시 + 안내.
4. **일별 백업**: 하루 첫 저장 시 `백업/erp-backup-YYYY-MM-DD.json`을 추가로 기록한다.
   백업은 최근 30개까지만 유지하고 오래된 것은 삭제한다.
5. 파일 형식: JSON (`JSON.stringify(state, null, 2)`), UTF-8. 스키마 버전 필드 `schemaVersion` 포함.
   데이터 폴더 안에는 다음 하위 폴더를 프로그램이 자동 생성한다:
   `백업/`(일별 백업), `증빙/`(영수증 원본 파일), `세무사전달/`(전달용 패키지).
   증빙 이미지·PDF는 JSON에 넣지 않고 **파일 그대로 폴더에 저장**하고 JSON에는 경로만 기록한다.
6. 설정 화면에 **수동 내보내기/가져오기**(JSON 다운로드·업로드)와
   **엑셀용 CSV 내보내기**(UTF-8 BOM `﻿` 포함, 표별 1파일) 버튼 제공.
7. 여러 PC에서 동시에 열면 충돌할 수 있으므로, 파일의 `lastSaved` 값이 메모리보다 최신이면
   "다른 곳에서 수정된 데이터가 있습니다" 경고를 띄우고 불러오기/덮어쓰기를 선택하게 한다.

## 4. 모듈 시스템 (설정에서 기능 켜고 끄기 — 핵심 요구사항)

업종마다 필요한 기능이 다르므로(서비스업은 재고 불필요, 판매업은 재고·배송 필수),
**모듈 단위로 켜고 끌 수 있는 구조**로 만든다.

### 4.1 모듈 목록

| 모듈 ID | 이름 | 기본값 | 비고 |
|---|---|---|---|
| `dashboard` | 대시보드 | 항상 켜짐 | 끌 수 없음 |
| `partners` | 거래처 관리 | 항상 켜짐 | 끌 수 없음 |
| `settings` | 데이터·설정 | 항상 켜짐 | 끌 수 없음 |
| `sales` | 매출(판매) 관리 | 켜짐 | |
| `purchases` | 매입(구매) 관리 | 켜짐 | |
| `inventory` | 품목·재고 관리 | 켜짐 | 끄면 매출·매입에서 품목 선택 대신 자유 입력 |
| `shipping` | 배송·출고 관리 | 꺼짐 | 상품 판매 회사용. `inventory` 필요 |
| `documents` | 견적서·거래명세서 발행 | 켜짐 | 거래명세표 양식은 6.9 참고(사용자 예시 제공 예정) |
| `payments` | 수금·지급 관리 (미수금·잔금) | 켜짐 | `sales` 또는 `purchases` 필요 |
| `reports` | 회계 리포트 (손익·원장·미수금 현황) | 켜짐 | |
| `expenses` | 경비(지출) 관리 | 꺼짐 | 간단한 지출 기록부 |
| `receipts` | 증빙(영수증) 관리 · 세무사 전달 | 꺼짐 | 영수증 파일 보관 + 전달용 패키지 생성 |
| `ledger` | 장부·자체기장 (매출장/매입장/현금출납부/연마감) | 켜짐 | 누적 기록·연도 이월 |

### 4.2 동작 규칙

- 설정 화면에 모듈별 토글 스위치를 두고, 끄면 **사이드바 메뉴에서 즉시 사라진다**.
- 모듈을 꺼도 **기존 데이터는 절대 삭제하지 않는다**. 다시 켜면 그대로 보인다.
- 의존 관계: `shipping`은 `inventory`가 켜져 있어야 켤 수 있다 (꺼져 있으면 토글 비활성 + 사유 표시).
- 대시보드는 켜져 있는 모듈의 위젯만 표시한다 (예: shipping이 꺼져 있으면 배송 현황 위젯 숨김).
- 구현은 모듈 레지스트리 패턴으로 한다:
  ```js
  const MODULES = {
    sales: { name: "매출 관리", icon: "💰", locked: false, requires: [],
             render: renderSales, dashboardWidgets: [...] },
    ...
  };
  ```
  새 모듈을 추가할 때 레지스트리에 등록만 하면 메뉴·설정·대시보드에 자동 반영되는 구조로 만든다.

## 5. 데이터 스키마

```jsonc
{
  "schemaVersion": 1,
  "lastSaved": "2026-07-29T09:00:00+09:00",
  "company": {            // 설정에서 입력, 문서 발행에 사용
    "name": "", "ceo": "", "bizNumber": "", "address": "",
    "phone": "", "email": "", "bankAccount": ""
  },
  "modules": { "sales": true, "purchases": true, "inventory": true,
               "shipping": false, "documents": true, "expenses": false },
  "partners": [{
    "id": "p1", "name": "", "type": "매출처|매입처|둘다",
    "bizNumber": "", "ceo": "", "phone": "", "email": "", "address": "",
    "openingBalance": 0,   // 기초 이월 미수금(+) 또는 미지급금(−)
    "memo": ""
  }],
  "items": [{
    "id": "i1", "code": "SKU-001", "name": "", "spec": "", "unit": "개",
    "salePrice": 0, "costPrice": 0,
    "baseStock": 0,          // 기초 재고
    "safeStock": 0,          // 안전 재고(이하로 떨어지면 경고)
    "memo": ""
  }],
  "sales": [{
    "id": "s1", "date": "2026-07-29", "partnerId": "p1",
    "lines": [{ "itemId": "i1", "name": "", "qty": 1, "unitPrice": 0 }],
    "supply": 0, "vat": 0, "total": 0,   // 공급가액/부가세/합계
    "vatIncluded": true,
    "status": "주문접수|출고완료|완료",
    "payments": [{ "date": "", "amount": 0, "method": "현금|계좌이체|카드|기타", "memo": "" }],
    // 수금액 = payments 합계, 미수금 = total − 수금액 (저장하지 않고 항상 계산)
    "memo": ""
  }],
  "purchases": [{ "...": "sales와 동일 구조(payments는 지급 내역)", "status": "발주|입고완료" }],
  "shipments": [{
    "id": "sh1", "saleId": "s1", "date": "",
    "receiver": "", "phone": "", "address": "",
    "courier": "", "trackingNo": "",
    "status": "상품준비|출고완료|배송중|배송완료",
    "memo": ""
  }],
  "expenses": [{ "id": "e1", "date": "", "category": "", "desc": "", "amount": 0, "memo": "" }],
  "receipts": [{
    "id": "r1", "date": "2026-07-29",
    "vendor": "",                       // 거래처/상호 (자유 입력)
    "amount": 0, "vat": 0,
    "evidenceType": "세금계산서|계산서|카드전표|현금영수증|간이영수증|기타",
    "category": "",                     // 경비 분류와 동일한 분류 사용
    "filePath": "증빙/2026-07/20260729_스타문구_33000.jpg",  // 데이터 폴더 기준 상대 경로
    "linkedTo": { "kind": "expense|purchase|sale|none", "id": "" },  // 장부 기록과 연결(선택)
    "memo": ""
  }],
  "documents": [{
    "id": "d1", "type": "quote|statement",   // 견적서 | 거래명세서
    "no": "Q-20260729-01", "date": "", "partnerId": "",
    "lines": [{ "name": "", "spec": "", "qty": 0, "unitPrice": 0 }],
    "memo": ""
  }],
  "cashOpening": 0,                     // 현금출납부 기초 시재
  "cashEntries": [{                     // 수동 입출금(가수금, 인출 등). 수금·지급·경비는 자동 반영
    "id": "c1", "date": "", "kind": "입금|출금", "amount": 0, "desc": "", "memo": ""
  }],
  "fiscalYear": 2026,                   // 현재 작업 연도
  "seq": { "quote": 1, "statement": 1 }      // 문서 일련번호
}
```

**재고 계산 규칙(저장하지 않고 항상 계산)**:
`현재고 = baseStock + (입고완료된 매입 수량 합) − (출고완료 이후 상태의 매출 수량 합) ± 재고조정`
재고조정은 `adjustments` 배열(품목ID, 날짜, 수량±, 사유)로 기록한다 — 재고실사 후 맞추는 용도.

**미수금·잔금 계산 규칙(저장하지 않고 항상 계산)**:
- 매출 건별 미수금 = `total − payments 합계` (0이면 "완납" 배지)
- 거래처별 미수금 잔액 = 그 거래처 모든 매출 건 미수금의 합
- 기준일 잔금(거래명세표용) = `기준일 이전 잔액(전잔금) + 당일 매출 합계 − 당일 입금액`
- 매입 쪽도 같은 방식으로 미지급금을 계산한다
- 거래처에 `openingBalance`(기초 이월 잔액, 프로그램 도입 전 미수금) 필드를 추가하고 잔액 계산에 포함한다

## 6. 화면(모듈)별 상세 기능

### 6.1 대시보드
- 상단 통계 타일: 이번 달 매출액 / 매입액 / 차익(매출−매입) / **미수금 총액** / 미완료 배송 건수
- **최근 12개월 매출·매입 막대 차트** (SVG 직접 그리기, 라이브러리 금지)
  - 매출 파랑 `#2a78d6`, 매입 주황 `#eb6834`. 범례 표시, 마우스 오버 시 툴팁으로 월·금액 표시
  - 다크모드에서는 `#3987e5` / `#d95926`
- 재고 부족 경고: 현재고 ≤ 안전재고인 품목 목록 (inventory 켜진 경우)
- 배송 진행 현황: 상태별 건수 요약 (shipping 켜진 경우)
- 최근 거래 10건 목록

### 6.2 매출 관리 (`sales`)
- 입력 폼: 날짜(기본 오늘), 거래처 선택, 품목 여러 줄 추가(품목 선택 시 판매가 자동 입력),
  수량, 단가, 부가세 자동 계산(공급가액×10%, 원단위 반올림), 부가세 미적용 체크
- 목록: 기간 필터(이번 달 기본), 거래처 필터, 검색. 수정·삭제 가능
- 목록에 결제 상태 배지: 완납 / 부분수금 / 미수. 건별 미수금 표시
- 매출 등록 시 shipping 모듈이 켜져 있으면 "배송 등록" 버튼으로 바로 배송 건 생성
- 목록에서 해당 건으로 **거래명세서 바로 발행** 버튼과 **수금 입력** 버튼

### 6.3 매입 관리 (`purchases`)
- 매출과 동일 구조. 상태: `발주` → `입고완료`. **입고완료 시점에만 재고에 반영**
- 품목 선택 시 매입가 자동 입력

### 6.4 품목·재고 관리 (`inventory`)
- 품목 등록: 품목코드, 품명, 규격, 단위, 판매가, 매입가, 기초재고, 안전재고
- 재고 현황표: 품목별 현재고, 안전재고, 상태 배지(정상/부족/품절), 재고 금액(현재고×매입가)
- 품목별 **입출고 이력 보기** (매입·매출·조정 내역을 시간순으로)
- **재고조정** 기능: 실사 후 수량 보정(± 수량, 사유 필수)

### 6.5 배송·출고 관리 (`shipping`)
- 배송 건: 매출 건에서 생성하거나 직접 등록. 수령인/연락처/주소/택배사/송장번호
- 상태 흐름: `상품준비` → `출고완료` → `배송중` → `배송완료` (버튼 클릭으로 단계 변경)
- **출고완료로 바뀌는 순간 재고 차감**이 확정된다 (매출 status와 연동)
- 목록: 상태 필터 탭(전체/상품준비/출고완료/배송중/배송완료), 검색(수령인·송장번호)
- 송장번호 클릭 시 택배사 조회 URL 새 창으로 열기 (CJ대한통운, 우체국, 롯데, 한진, 로젠 정도 매핑)

### 6.6 거래처 관리 (`partners`)
- 등록: 상호, 구분(매출처/매입처/둘다), 사업자번호, 대표자, 연락처, 이메일, 주소, 메모
- 목록 + 검색. 거래처 클릭 시 해당 거래처의 거래 이력 요약(총 매출, 총 매입, 최근 거래일)

### 6.7 수금·지급 관리 (`payments`)
- **수금 입력**: 매출 건을 선택해 수금 기록(날짜, 금액, 방법). **부분 수금 지원** —
  예: 합계 1,000,000원 중 계약금 300,000원 수금 → 잔금 700,000원이 미수금으로 남는다
- 초과 수금 입력 시 경고(미수금보다 큰 금액)
- **미수금 현황**: 거래처별 미수금 잔액 목록(기초 이월 포함), 클릭 시 건별 상세
- 미수금 연령 분석: 30일 미만 / 30~60일 / 60~90일 / 90일 이상 구간별 금액
- 매입 쪽 **지급 관리**(미지급금)도 동일한 구조로 제공
- 거래처별 원장 보기: 기간 내 매출·수금을 시간순으로 나열하고 잔액을 누적 계산
  (전잔금 → 거래 → 입금 → 잔금 흐름. 거래명세표의 잔금 칸과 같은 논리)

### 6.8 회계 리포트 (`reports`)
- **월별 손익 요약**: 매출(공급가액) − 매입(공급가액) − 경비 = 이익. 표 + 12개월 추이
- **거래처별 매출/매입 순위** (기간 선택)
- **부가세 참고 자료**: 기간별 매출 부가세 합계, 매입 부가세 합계 (신고용이 아닌 참고용임을 명시)
- 모든 리포트는 CSV로 내려받기 가능
- 화면 상단에 고정 문구: "본 리포트는 내부 관리용이며 세무 신고 자료가 아닙니다."

### 6.9 견적서·거래명세서 (`documents`)
- 유형 선택(견적서/거래명세서), 거래처 선택, 품목 줄 추가, 합계 자동 계산
- 일련번호 자동 채번: `Q-YYYYMMDD-01`, `S-YYYYMMDD-01`
- **인쇄 미리보기 → 브라우저 인쇄(PDF 저장)**. A4 기준
- 견적서: 공급자(회사 정보)/공급받는자 박스, 품목 표, 합계금액(한글 금액 병기: 예 "일금 일백이십만원整")
- **거래명세표: 사용자의 실물 양식을 기준으로 구현한다. 정확한 명세는 12장 참조.**
  전잔금(전미수금) / 당일 합계 / 입금 / 총미수잔액 값은 payments 모듈의 잔액 계산 규칙을 사용
- 발행 이력 저장, 재인쇄 가능

### 6.10 경비 관리 (`expenses`)
- 날짜, 분류(식대/교통/소모품/임차료/기타 — 분류는 설정에서 추가 가능), 내용, 금액
- 월별 분류별 합계 표

### 6.11 증빙(영수증) 관리 · 세무사 전달 (`receipts`)

**목적**: 영수증·세금계산서 등 증빙을 한곳에 모아두고, 분기·연말에 세무사(기장 대리인)에게
넘길 자료를 클릭 몇 번으로 만들어 주는 기능. 세무 "신고"가 아니라 자료 "정리·전달"까지만 한다.

- **증빙 업로드**: 파일 선택 또는 드래그&드롭(이미지 jpg/png, PDF 지원).
  업로드하면 데이터 폴더의 `증빙/YYYY-MM/` 폴더에 복사하고,
  파일명을 `YYYYMMDD_상호_금액.확장자` 형식으로 자동 변경한다 (예: `20260729_스타문구_33000.jpg`).
  휴대폰으로 찍은 사진은 구글드라이브 모바일 앱으로 증빙 폴더에 올린 뒤
  프로그램에서 "폴더에서 가져오기"로 등록할 수도 있게 한다.
- **정보 입력**: 날짜, 상호, 금액, 부가세, 증빙 유형(세금계산서/계산서/카드전표/현금영수증/간이영수증/기타), 분류, 메모.
  OCR·자동 인식은 만들지 않는다(외부 API 필요) — 수기 입력 기준으로 설계하되 입력 폼을 빠르게 만든다
  (직전 입력값 기억, 상호 자동완성).
- **장부 연결**: 증빙을 경비·매입·매출 기록과 연결할 수 있다(선택 사항).
  경비 입력 화면에도 "증빙 첨부" 버튼을 두어 양방향으로 연결 가능하게 한다.
- **목록·검색**: 월별 필터, 유형 필터, 상호 검색. 썸네일 미리보기, 클릭 시 원본 보기
  (파일 핸들에서 `createObjectURL`로 표시). **증빙 없는 경비 목록**도 보여준다(누락 점검용).
- **세무사 전달 패키지 만들기 (핵심 기능)**:
  1. 기간 선택(월/분기/반기/직접 지정)
  2. `세무사전달/YYYY-MM~YYYY-MM/` 폴더를 만들고 다음을 자동 생성:
     - 해당 기간 증빙 파일 전체 사본 (월별 하위 폴더로 정리)
     - `증빙목록.csv` — 날짜, 상호, 금액, 부가세, 유형, 분류, 파일명, 연결된 장부 기록
     - `매출내역.csv`, `매입내역.csv`, `경비내역.csv` (해당 기간)
     - `요약.txt` — 기간, 건수, 금액 합계, 증빙 누락 건수
  3. 완료 후 안내: "이 폴더는 구글드라이브에 자동 동기화됩니다.
     구글드라이브에서 이 폴더의 공유 링크를 만들어 세무사에게 보내세요."
- 화면에 고정 문구: "증빙 원본(종이)은 법정 보존기간(5년) 동안 별도 보관하세요."

### 6.12 장부·자체기장 (`ledger`)

**목적**: 세무사 기장을 맡기지 않거나 병행하는 회사가 **자체기장 장부**로 쓸 수 있게,
모든 거래를 누적 기록 형태(전통 장부 양식)로 보여주고 연 단위로 마감·이월한다.

- **누적 기록 원칙**: 모든 데이터는 삭제하지 않는 한 계속 쌓인다. 여러 해 데이터를 보관하며,
  모든 목록 화면에 연도·기간 선택을 제공한다.
- **매출장 / 매입장**: 일자순으로 전 건 나열. 각 행에 거래처, 품목 요약, 공급가액, 부가세, 합계를
  표시하고 **일계(하루 합) · 월계(월 합) · 누계(연초부터 누적)** 행을 자동 삽입한다.
  월별로 접기/펼치기 가능. 인쇄(A4)와 CSV 내려받기 지원.
- **현금출납부**: 날짜순 입금/출금/잔액 3단 구성. 기초 시재(`cashOpening`)에서 시작해
  수금(입금)·지급(출금)·경비(출금)를 **자동으로 반영**하고, 장부 외 입출금(대표자 가수금,
  현금 인출 등)은 수동 등록. 잔액은 누적 계산. 일계·월계·누계 표시.
- **거래처 원장**: 6.7의 원장 보기를 이 모듈에서도 진입 가능하게 한다 (전잔금→거래→입금→잔금 누적).
- **연도 마감(이월)**:
  1. 설정한 회계연도(1/1~12/31)가 끝나면 "연도 마감" 버튼 활성화
  2. 마감 실행 시: 거래처별 미수금·미지급금 잔액 → 다음 해 `openingBalance`로 이월,
     품목별 현재고 → 다음 해 `baseStock`으로 이월, 현금 잔액 → 다음 해 `cashOpening`으로 이월
  3. 지난 해 전체 데이터는 `장부보관/erp-2026.json`으로 아카이브하고, 작업 파일은 새 연도로 시작
  4. 아카이브된 연도는 프로그램에서 연도 선택으로 **읽기 전용 조회** 가능 (수정 불가)
  5. 마감은 2단계 확인 + 마감 직전 자동 백업. 마감 취소(직전 1회 되돌리기)도 제공
- 연도 마감을 하지 않고 한 파일에 계속 쌓는 것도 허용한다(마감은 선택 기능).

### 6.13 데이터·설정 (`settings`)
- 회사 정보 입력(문서 발행용)
- **모듈 켜기/끄기 토글** (4장 규칙대로)
- 데이터 폴더 연결 상태 및 재연결, 마지막 저장/백업 시각 표시
- JSON 내보내기/가져오기, CSV 내보내기
- **예시 데이터 넣기** 버튼(시연용 더미 데이터 30건 내외 — 실제 회사명 사용 금지) / 전체 데이터 초기화(2단계 확인)

## 7. UI/UX 가이드

- 왼쪽 어두운 사이드바(메뉴) + 오른쪽 밝은 콘텐츠 영역. 시스템 폰트 사용
  (`system-ui, -apple-system, "Segoe UI", "Malgun Gothic", sans-serif`)
- 라이트/다크 모드: `prefers-color-scheme` 자동 대응. CSS 변수로 색상 토큰화
- 표의 금액 칸은 오른쪽 정렬 + `font-variant-numeric: tabular-nums`
- 모든 삭제는 확인 대화상자. 저장 성공/실패는 토스트 메시지로 피드백
- 모바일(760px 이하)에서도 깨지지 않게 반응형 처리
- 인쇄 시(`@media print`) 문서 영역만 출력되도록 처리

## 8. 검증 체크리스트 (개발 완료 조건)

- [ ] 폴더 연결 → 데이터 입력 → 브라우저 완전 종료 → 재실행 → 데이터 복원 확인
- [ ] 매입(입고완료) 10개 → 매출 3개(출고완료) → 현재고가 기초재고+7인지 확인
- [ ] shipping 모듈: 출고완료 전에는 재고가 줄지 않는지 확인
- [ ] 모듈을 껐다 켰을 때 데이터가 보존되는지 확인
- [ ] 견적서 인쇄 미리보기가 A4 한 장에 올바르게 나오는지 확인
- [ ] 부가세 계산: 공급가액 30,500원 → 부가세 3,050원 → 합계 33,550원
- [ ] 수금: 매출 1,000,000원 → 수금 300,000원 → 미수금 700,000원, 배지 "부분수금" 확인
- [ ] 잔금 계산: 전잔금 500,000 + 당일 매출 200,000 − 입금 600,000 = 잔금 100,000 확인
- [ ] 거래처별 미수금 합계가 건별 미수금의 합 + 기초 이월과 일치하는지 확인
- [ ] 증빙 업로드 시 `증빙/YYYY-MM/` 폴더에 규칙대로 이름이 바뀐 파일이 생기는지 확인
- [ ] 세무사 전달 패키지: 폴더 구조 + CSV 4종 + 요약.txt가 생성되고 CSV 합계가 화면과 일치하는지 확인
- [ ] 증빙 없는 경비가 "누락 점검" 목록에 나타나는지 확인
- [ ] 매출장의 월계·누계가 개별 건 합계와 일치하는지 확인
- [ ] 현금출납부: 기초 시재 100,000 + 입금 50,000 − 출금 30,000 = 잔액 120,000 확인
- [ ] 연도 마감 후 거래처 잔액·재고·현금 시재가 새 연도 기초값으로 정확히 이월되는지,
      아카이브 연도가 읽기 전용으로 조회되는지 확인
- [ ] CSV를 엑셀에서 열었을 때 한글이 깨지지 않는지 확인(BOM)
- [ ] Firefox에서 폴백 모드 배너와 수동 저장이 동작하는지 확인
- [ ] 설치형(.exe): 설치 → 같은 구글드라이브 폴더 지정 → 웹형에서 입력한 데이터가 그대로 보이는지,
      설치형에서 수정한 내용이 웹형에서도 보이는지(파일 호환) 확인

## 9. 배포 준비물 (프로그램 완성 후 추가 지시)

- `README.md`: 스크린샷, 기능 소개, 사용법(구글드라이브 폴더 연결 방법 포함), FAQ,
  두 가지 사용 방법 안내: ① HTML 파일 바로 실행(+크롬 "앱으로 설치" 방법) ② 설치형 .exe 다운로드
- 배포 파일: `미니ERP.html`(웹형), `미니ERP-Setup-x.x.x.exe`(설치형) — GitHub Releases에 함께 업로드
- `LICENSE`: MIT
- 사용 설명서: 비전문가용 그림 위주 가이드 (별도 md 또는 html)
- 면책 문구: "본 프로그램은 세무 신고용 장부가 아니며, 공식 신고는 세무 전문가와 확인하세요."

## 10. 개발 시 지켜야 할 원칙

1. 한 단계씩 완성하고 동작 확인 후 다음 단계로 넘어간다 (한 번에 전부 만들지 않는다).
2. 데이터를 파괴할 수 있는 코드(초기화, 덮어쓰기)는 반드시 확인 절차를 거친다.
3. 기능 추가보다 **데이터 안전(자동저장·백업·복구)**이 항상 우선이다.
4. 이 지시서에 없는 기능을 임의로 추가하지 말고, 제안만 하고 확인을 받는다.

## 11. 개발 단계 (이 순서대로 진행)

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| 1 | 레이아웃 + 모듈 레지스트리 + 설정 토글 | 메뉴가 토글에 따라 나타나고 사라짐 |
| 2 | 데이터 저장 계층 (폴더 연결·자동저장·백업·폴백) | 체크리스트 1번 통과 |
| 3 | 거래처 + 품목·재고 CRUD | 등록·수정·삭제·검색 동작 |
| 4 | 매입 + 매출 입력, 재고 연동 | 체크리스트 2번 통과 |
| 5 | 배송·출고 모듈 | 체크리스트 3번 통과 |
| 6 | 수금·지급 모듈 (부분 수금, 미수금, 잔금, 원장) | 수금·잔금 체크리스트 통과 |
| 7 | 대시보드 (타일 + 12개월 차트 + 경고 + 미수금) | 차트 툴팁까지 동작 |
| 8 | 견적서·거래명세서 발행/인쇄 (거래명세표는 임시 양식) | 인쇄 체크리스트 통과 |
| 9 | 회계 리포트 + 경비 모듈 + CSV/JSON 내보내기 | CSV 체크리스트 통과 |
| 10 | 증빙(영수증) 관리 + 세무사 전달 패키지 | 증빙 체크리스트 3개 통과 |
| 11 | 장부·자체기장 (매출장/매입장/현금출납부/연도 마감) | 장부 체크리스트 3개 통과 |
| 12 | 예시 데이터, 반응형·다크모드 다듬기, 전체 검증 | 체크리스트 전체 통과 |
| 13 | 단일 파일 번들 + README 등 배포 준비물 (웹형 완성) | GitHub 업로드 가능 상태 |
| 14 | **Electron 설치형 빌드**: storage 어댑터 분리 → preload/IPC 구현 → .exe 설치 파일 | 웹형과 같은 데이터 폴더로 호환 동작, 설치·실행 확인 |
| 15 | 거래명세표를 12장 양식 명세대로 구현 | 실물 예시와 동일한 인쇄 출력 |

---

## 12. 거래명세표 양식 명세 (사용자 실물 엑셀 기준)

사용자가 현재 엑셀로 쓰고 있는 거래명세표를 분석한 결과다. **인쇄 결과가 이 양식과 같아야 한다.**

### 12.1 전체 구성

- **A4 가로(landscape) 1장에 같은 명세서 2부를 좌우로 나란히 인쇄**
  - 왼쪽: `거래명세서 (공급자)` — 공급자 보관용
  - 오른쪽: `거래명세서 (공급받는자)` — 공급받는자 보관용
  - 두 부는 제목의 괄호 문구만 다르고 내용은 완전히 동일(오른쪽은 왼쪽을 그대로 복제)
- 모든 칸은 실선 테두리의 표 형태. 흑백 인쇄 기준.

### 12.2 상단 영역 (각 부 공통)

| 항목 | 내용 |
|---|---|
| 제목 | `거 래 명 세 서` + 줄바꿈 + `( 공 급 자 )` 또는 `( 공 급 받 는 자 )` |
| 작성일자 | 발행일 (기본값: 오늘) |
| 공급받는자 | 거래처 상호 + ` 귀하` (크게, 왼쪽 배치) |
| 공급자 박스 | 상호 / 성명(대표자) / 사업자번호 / 계좌(은행+계좌번호+예금주) / 주소 / 전화번호 — **설정의 회사 정보에서 자동 입력** |
| 안내 문구 | `아래와 같이 공급합니다.` |
| 합계(상단) | 품목 합계 금액 + `원정` 표기 (예: `363,000 원정`) |

### 12.3 품목 표

- 열 구성: **품명 | 규격 | 수량 | 단가 | 공급가액 | 기타**
- 기본 12행. 품목이 12개를 넘으면 다음 장으로 넘긴다(각 장마다 동일 양식).
- **단가 자동 입력**: 품명 선택 시 품목의 판매 단가를 자동으로 가져온다
  (사용자의 기존 엑셀에서 `VLOOKUP(품명, 단가표)`으로 하던 것과 같은 동작.
  단가표의 도매가에 해당). 수동으로 덮어쓸 수 있다.
- 공급가액 = 수량 × 단가. **부가세를 별도 표시하지 않는 단가 포함 방식** —
  이 문서에서는 공급가액 열 하나만 사용한다.
- 마지막 행 `계`: **모든 품목 행의 공급가액 합계.**
  (주의: 기존 엑셀은 SUM 범위가 고정이라 행 추가 시 합계 누락 오류가 있었다.
  프로그램에서는 전체 행을 항상 포함해 이 오류를 원천 차단한다.)

### 12.4 하단 정산 영역 (미수금 연동 — payments 모듈)

| 칸 | 계산 |
|---|---|
| 전잔금 | 발행일 이전 그 거래처의 미수금 잔액 (payments 모듈에서 자동. 수동 수정 가능) |
| 합계 | 계(당일 품목 합계) + 전잔금 |
| 입금 | 당일 입금액 (수금 기록에서 자동, 수동 수정 가능) |
| 총미수잔액 | 합계 − 입금 |

- 거래명세표를 발행하면 해당 매출 건과 연결되고, 표시된 전잔금·입금·총미수잔액은
  발행 시점 값으로 문서에 **고정 저장**한다(나중에 수금이 늘어도 발행분은 변하지 않음. 재발행 시 갱신 선택).

### 12.5 옵션

- **공제(할인) 기능**: 사용자의 엑셀에 `5%공제` 변형 시트가 있다. 거래처별 또는 발행 건별로
  공제율(%)을 설정하면 `계` 아래에 `공제` 행을 추가하고 합계에서 차감하도록 옵션으로 구현한다.
- 발행 화면에서 "공급자 보관용만 / 2부(기본)" 선택 가능하게 한다.

---

### Claude Code에 입력할 첫 프롬프트 (복사용)

```
SPEC.md(미니 ERP 개발 지시서)를 읽고 그대로 개발해줘.
11장의 개발 단계 순서를 지키고, 각 단계가 끝나면 완료 보고와 함께
8장 검증 체크리스트 중 해당 항목을 직접 테스트한 결과를 보여줘.
지시서에 없는 기능은 임의로 추가하지 말고 먼저 제안해줘.
```
